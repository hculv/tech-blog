<div class="text-center">
  <h2>{{name}}</h2>
  <p>{{{get_emoji}}}</p>
  <p>{{description}}</p>
  <p>Created by {{user.name}} on {{format_date date_created}}</p>
  {{!-- <p>
    <span for="img" aria-label="money">💲</span>
    <span class="dollar-amount">{{format_amount needed_funding}}</span>
    needed
  </p> --}}
  
  {{!-- <form class="form comment-form">
      <div class="form-group-comment" id="comment-form">
        <label> Add a comment here:</label>
        <input class="form-input" type="text" id="comment-body" name="comment-body" value="{{comment.description}}"/>
      </div>
      </form>
  <p>
    <button id="comment-button" class="btn btn-primary" type="submit"> Comment on this blog!</button>
  </p>
</div> --}}


	{{#if logged_in}}

		<form class="new-comment-form formCont d-flex flex-wrap justify-content-center" data-blogid={{id}}>
			{{!-- <div class="textAreaCont p-2"> --}}
        <div>
				<textarea id='comment_description' name="comment-body" type="text"
				placeholder="Enter a comment here..."
				cols="30"
				rows="2"></textarea>
			</div>
			<div class="btnComment p-2">
				<button class="button btn btn-success" type="submit">Add Comment</button>
			</div>
		</form>


		{{/if}}

		<h2 class="text-center comment-header">Comments</h2>
    <ul>
      <li id="commentList"></li>
    </ul>
		{{#each comments as |comment|}}
		<div>
			<p class="text-center" id="comment.descrip">{{comment.description}}</p>
			<p class="text-center"><span>~</span>Created by {{comment.user.username}}<span>~</span></p>
		</div>
		{{/each}}

  {{!-- <section id="comments">

      {{#each this}}
  <div>
    <p>
    {{comment.description}} 
    </p>
  </div>
  {{/each}}
    <!-- For each painting that belongs to this gallery, the painting-details partial is used to render each painting's data -->
    {{!-- {{#each projects as |project|}}
    {{> comment-description}}
    {{/each}} --}}
  {{!-- </section>
</section> --}} 

<script src="/js/comment.js"></script>



