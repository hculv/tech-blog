<div class="text-center">
  <h2>{{name}}</h2>
  <p>{{{get_emoji}}}</p>
  <p>{{description}}</p>
  <p>Created by {{user.name}} on {{format_date date_created}}</p>
  <p>
    <span for="img" aria-label="money">💲</span>
    <span class="dollar-amount">{{format_amount needed_funding}}</span>
    needed
  </p>
  
  <form class="form new-comment-form">
      <div class="form-group">
        <label for="comment-body"></label>
        <input class="form-input" type="text" id="comment-body" name="comment-body" />
      </div>
      </form>
  <p>
    <button class="btn btn-primary" type="submit"> Comment on this blog!</button>
  </p>
</div>


  <section class="flex-row justify-center">
    <!-- For each painting that belongs to this gallery, the painting-details partial is used to render each painting's data -->
    {{#each projects as |project|}}
    {{> comment-body}}
    {{/each}}
  </section>
</section>

<script src="/js/comment.js"></script>
